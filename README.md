# A receipt input and printing app

an app where one can enter details and automatically generate a receipt

the receipt is for mining records.

the app should allow going back to prevoius entries and view their details

app data can be stored in firebase

app should allow connect to printer and printing automatically
